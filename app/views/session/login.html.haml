%h1= 'נא לבחור שיטת אימות'

= form_tag session_do_login_path do
  %div{style:'width: 200px'}
    = link_to '/auth/google_oauth2', method: :post do
      .btn-small-outline-v02.static-btn= 'Google'
    - if Rails.env.development?
      = link_to '/auth/developer', method: :post do
        .btn-small-outline-v02.static-btn= 'Developer'

