%div{align: 'right', dir: 'rtl'}
  %p= "#{@tag.creator&.name || t('admin.tags.unknown_creator')}, #{@greeting}"

  %p= t(:tag_approved_intro, tag: @tag.name)

  %hr

  != email_footer_html(@tag.creator&.email)
