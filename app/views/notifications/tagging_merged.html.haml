%div{align: 'right', dir: 'rtl'}
  %p= "#{@tagging_suggester}, #{@greeting}"
  %p= t(:tagging_merged_email_html, tag: @tagging.tag.name, original_tag: @original_tagname, taggable: @tagging.taggable.title, url: polymorphic_url(@tagging.taggable))

  %hr

  != email_footer_html(@recipient_email)
