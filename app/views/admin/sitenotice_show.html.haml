.backend
  %h1= t(:sitenotice)
  %a{ href: sitenotice_edit_path(@sn)}
    .btn{style:'float:none'}
      %b= t(:edit)
  %br
  %br
  %p
    %b= t(:sitenotice_body)+': '
    != @sn.body
  %h3= t(:fromdate)
  %p= @sn.fromdate.strftime('%d-%m-%Y')
  %p= @sn.todate.strftime('%d-%m-%Y')
  - if @sn.enabled?
    %span{style:'color:green'}= t(:enabled)
  - else
    %span{style:'color:red'}= t(:disabled)
  = link_to t(:back), admin_sitenotice_list_path
