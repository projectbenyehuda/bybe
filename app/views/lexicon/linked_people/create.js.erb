<% unless @linked_person.new_record? %>
reloadLinkedPeople();
<% else %>
$('#new-linked-person-form').html(
  '<%= j render(partial: "lexicon/linked_people/form", locals: { work: @work, linked_person: @linked_person }) %>'
);
<% end %>
