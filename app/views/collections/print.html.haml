/ print mode.
.print
  #content.container-fluid
    .row.anthology-page-content
      = render partial: 'coll_body'

:javascript
  $(document).ready(function() {
    $(document).on('keyup',function(evt) {
      if (evt.keyCode == 27) {
        window.location.href = '#{collection_path(@collection)}';
      }
    });
    window.print();
  });
