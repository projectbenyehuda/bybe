%ol
  - links.each do |link|
    %li
      = link_to link.description, link.url
      = ' ('+t(link.linktype)+')'
      - if edit
        = link_to t(:destroy), manifestation_remove_link_path(link_id: link.id, id: @m.id), :data => { :confirm => t(:are_you_sure) }
